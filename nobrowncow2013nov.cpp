#include <iostream>
#include <
#include <unordered_set>

using namespace std;

int main(void){
    int n, k;
    cin >> n >> k;
    int words = 0;
    vector<unordered_set<int> > > (n) f;
    for(int i = 0; i < 30; i++){
        string a;
        cin >> a;
        if(a == "cow."){
            break;
        }else if(a != "Farmer" && a != "John" && a != "No"){

        }
        ++words;
    }
    for(int i = 0; i < n - 1; i++){
        for(int j = 0; j < words; ++j){
            
        }
    }
    for(int i = 0; i < n; i++){

    }
    return 0;
}